# Supabase project, Settings > API
# Leave empty to run the site in demo mode (predictions stored in the browser only)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Minimum $FABLE balance required to submit a prediction (display only for now)
NEXT_PUBLIC_MIN_FABLE=1000
